<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/LinearLayoutParent"
    android:background="@color/colorPrimaryDark"
    >
    <LinearLayout
        style="@style/LinearLayoutParent"
        android:background="@color/colorPrimaryDark"
        android:padding="8dp"
        android:orientation="vertical"
        tools:context=".DetailFragment">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            tools:ignore="UseCompoundDrawables">

            <ImageView
                android:id="@+id/iv_arrow_back_detail_fragment"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:padding="10dp"
                android:contentDescription="@string/arrow_back"
                android:src="@drawable/ic_arrow_back_black" />

            <TextView
                android:id="@+id/tv_title_detail_fragment"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:gravity="center"
                android:textStyle="bold"
                android:textSize="20sp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/iv_photo_detail_fragment"
            style="@style/DetailFragment"
            android:contentDescription="@string/attraction_location_photo"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/tv_text_detail_fragment"
            style="@style/DetailFragment"
            android:gravity="center_vertical" />

    </LinearLayout>
</ScrollView>
